import React, { useState } from 'react';\n\ninterface SearchBarProps {\n  onAddStock: (ticker: string) => Promise<void>;\n  loading: boolean;\n}\n\nconst SearchBar: React.FC<SearchBarProps> = ({ onAddStock, loading }) => {\n  const [input, setInput] = useState('');\n\n  const handleSubmit = async (e: React.FormEvent) => {\n    e.preventDefault();\n    if (!input.trim()) return;\n\n    await onAddStock(input.toUpperCase());\n    setInput('');\n  };\n\n  return (\n    <form onSubmit={handleSubmit} className=\"mb-8\">\n      <div className=\"flex gap-2\">\n        <input\n          type=\"text\"\n          value={input}\n          onChange={(e) => setInput(e.target.value)}\n          placeholder=\"Enter stock ticker (e.g., AAPL, GOOGL, MSFT)\"\n          className=\"flex-1 px-4 py-3 rounded-lg bg-slate-700 text-white placeholder-slate-400 border border-slate-600 focus:border-blue-500 focus:outline-none\"\n          disabled={loading}\n        />\n        <button\n          type=\"submit\"\n          disabled={loading}\n          className=\"px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg disabled:opacity-50 disabled:cursor-not-allowed transition\"\n        >\n          {loading ? 'Adding...' : 'Add Stock'}\n        </button>\n      </div>\n    </form>\n  );\n};\n\nexport default SearchBar;\n